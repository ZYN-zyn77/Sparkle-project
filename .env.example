# Database Configuration
DB_USER=postgres
DB_PASSWORD=change-me
DB_NAME=sparkle

# Redis Configuration
REDIS_PASSWORD=change-me

# Security
JWT_SECRET=change-me-in-production

# LLM Configuration
LLM_API_BASE_URL=
LLM_API_KEY=

# Note: Never commit actual secrets to version control
# Create a .env.local file locally for development secrets
# Ensure .env.local is added to .gitignore
