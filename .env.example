# ==========================================
# Sparkle Docker Environment Configuration
# ==========================================
# 使用说明：复制此文件为 .env 并根据需要修改
# 统一开发密码：devpassword（生产环境必须使用强密码）

# Database Configuration
DB_USER=postgres
DB_PASSWORD=devpassword
DB_NAME=sparkle

# Redis Configuration
REDIS_PASSWORD=devpassword

# MinIO Configuration
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_BUCKET=sparkle-files

# Container URLs (可选覆盖)
# DATABASE_URL=postgresql://postgres:devpassword@sparkle_db:5432/sparkle
# REDIS_URL=redis://:devpassword@sparkle_redis:6379/0
# CELERY_BROKER_URL=redis://:devpassword@sparkle_redis:6379/1
# CELERY_RESULT_BACKEND=redis://:devpassword@sparkle_redis:6379/2

# Flower (Celery 监控)
FLOWER_IMAGE=mher/flower:1.2.0

# Gateway Secrets (生产环境必填)
JWT_SECRET=dev-jwt-secret
ADMIN_SECRET=dev-admin-secret

# Backend Internal Key (用于 Gateway <-> Backend 认证)
INTERNAL_API_KEY=dev-internal-key

# LLM API Configuration
LLM_API_KEY=your-llm-api-key-here

# ==========================================
# ⚠️ 安全提示 (Security Notice)
# ==========================================
# 1. 本配置仅用于开发环境
# 2. 生产环境必须使用强密码（推荐 32 字符随机字符串）
# 3. 生产环境建议使用 AWS Secrets Manager 或 HashiCorp Vault
# 4. 启用 PostgreSQL SSL: ?sslmode=require
# 5. 启用 Redis TLS
# ==========================================
