# Sparkle 生产部署指南

> **版本**: v1.1.0
> **架构**: 混合微服务 (Flutter + Go Gateway + Python Engine)
> **最后更新**: 2026-01-10

---

## 📋 部署清单

### ✅ 部署前检查

- [ ] **基础设施**: PostgreSQL (带 pgvector), Redis, MinIO/S3 已就绪
- [ ] **代码版本**: 确保 Go Gateway 和 Python Engine 代码版本一致
- [ ] **数据库**: Schema 已同步 (`make sync-db`)，向量索引已创建
- [ ] **密钥**: `JWT_SECRET` 和 `INTERNAL_API_KEY` 已生成且一致
- [ ] **网络**: 域名 SSL 证书已配置，防火墙端口已开放 (443, 80)

---

## 🔧 环境配置

### 1. 核心环境变量 (.env.production)

```bash
# ==================== 基础信息 ====================
ENVIRONMENT=production
TZ=Asia/Shanghai
LOG_LEVEL=info

# ==================== 数据库 & 缓存 ====================
# 使用连接池模式 (PgBouncer) 推荐端口 6432
DATABASE_URL=postgres://sparkle_user:strong_password@db_host:5432/sparkle
REDIS_URL=redis://:redis_password@redis_host:6379/0

# ==================== 内部通信安全 ====================
# 用于 Gateway <-> Python Engine 之间的互信
INTERNAL_API_KEY=sk_internal_super_secure_random_key_min_32_chars

# ==================== Python AI Engine ====================
PORT=8000
GRPC_PORT=50051
OPENAI_API_KEY=sk-xxx
# Celery Broker/Backend (通常复用 Redis)
CELERY_BROKER_URL=redis://:redis_password@redis_host:6379/1
CELERY_RESULT_BACKEND=redis://:redis_password@redis_host:6379/2

# ==================== Go Gateway ====================
GATEWAY_PORT=8080
JWT_SECRET=jwt_secret_key_min_32_chars_random
# 允许的 WebSocket 来源 (防跨站劫持)
ALLOWED_ORIGINS=https://app.sparkle.com,https://admin.sparkle.com
# 代理指向 Python Engine
AGENT_ADDRESS=python_engine_host:50051
BACKEND_URL=http://python_engine_host:8000

# ==================== 文件存储 (MinIO/S3) ====================
MINIO_ENDPOINT=s3.amazonaws.com
MINIO_ACCESS_KEY=your_access_key
MINIO_SECRET_KEY=your_secret_key
MINIO_BUCKET=sparkle-prod
MINIO_USE_SSL=true
```

---

## 🐳 Docker Swarm / Compose 部署

### 1. docker-compose.prod.yml

```yaml
version: '3.8'

services:
  # 1. 基础设施 (若使用云服务可移除)
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: sparkle_user
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: sparkle
    volumes:
      - pg_data:/var/lib/postgresql/data
    deploy:
      resources:
        limits:
          memory: 4G

  redis:
    image: redis:7-alpine
    command: redis-server --requirepass ${REDIS_PASSWORD}
    volumes:
      - redis_data:/data

  # 2. Python AI Engine (Core Logic)
  ai_engine:
    image: sparkle/ai-engine:latest
    build: 
      context: ./backend
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL}
      - INTERNAL_API_KEY=${INTERNAL_API_KEY}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: '2.0'
          memory: 2G
    depends_on:
      - db
      - redis

  # 3. Go Gateway (High Concurrency Entry)
  gateway:
    image: sparkle/gateway:latest
    build:
      context: ./backend/gateway
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL}
      - JWT_SECRET=${JWT_SECRET}
      - INTERNAL_API_KEY=${INTERNAL_API_KEY}
      - AGENT_ADDRESS=ai_engine:50051
      - BACKEND_URL=http://ai_engine:8000
      - ALLOWED_ORIGINS=${ALLOWED_ORIGINS}
    ports:
      - "8080:8080"
    deploy:
      replicas: 3
      resources:
        limits:
          cpus: '1.0'
          memory: 1G
    depends_on:
      - ai_engine

  # 4. Celery Workers (Async Tasks)
  celery_worker:
    image: sparkle/ai-engine:latest
    command: celery -A app.core.celery_app worker -l info -c 4
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL}
      - INTERNAL_API_KEY=${INTERNAL_API_KEY}
    deploy:
      replicas: 2

  celery_beat:
    image: sparkle/ai-engine:latest
    command: celery -A app.core.celery_app beat -l info
    deploy:
      replicas: 1

volumes:
  pg_data:
  redis_data:
```

### 2. 部署操作

```bash
# 1. 构建镜像
docker compose -f docker-compose.prod.yml build

# 2. 启动服务
docker compose -f docker-compose.prod.yml up -d

# 3. 执行数据库迁移
docker compose -f docker-compose.prod.yml exec ai_engine alembic upgrade head
```

---

## 🛡️ 安全加固 (Security Hardening)

### 1. 内部 API 鉴权 (P0)

Go Gateway 和 Python Engine 之间的 REST API 调用必须携带 `X-Internal-API-Key` 头。这是为了防止绕过网关直接攻击 Python 服务（虽然在 Docker 网络内部，但这是纵深防御的一环）。

**验证**:
确保 `.env.production` 中 `INTERNAL_API_KEY` 设置为强随机字符串。

### 2. WebSocket 来源检查 (P1)

Go Gateway 会校验 HTTP Header 中的 `Origin`。
在 `.env.production` 中配置 `ALLOWED_ORIGINS`:

```bash
ALLOWED_ORIGINS=https://sparkle.your-domain.com
```

### 3. gRPC TLS (可选但推荐)

如果 Gateway 和 Engine 跨主机部署，必须启用 gRPC TLS。

```bash
AGENT_TLS_ENABLED=true
AGENT_TLS_CA_CERT=/etc/certs/ca.pem
```

---

## 📊 运维监控 (Ops)

### 1. 关键指标 (Prometheus)

-   **Gateway**:
    -   `websocket_connections_active`: 当前在线连接数
    -   `http_request_duration_seconds`: API 延迟
-   **AI Engine**:
    -   `grpc_server_handled_total`: gRPC 调用次数
    -   `celery_task_latency`: 异步任务排队延迟

### 2. 日志分析 (Loki/ELK)

关注以下关键字:
-   `level=error`
-   `panic`
-   `connection refused`
-   `CircuitBreakerOpen` (熔断器开启)

---

## 🔄 备份与恢复

### 1. 数据库备份

使用 `pg_dump` 每天备份：

```bash
docker exec -t sparkle_db pg_dump -U postgres sparkle | gzip > /backups/sparkle_$(date +%Y%m%d).sql.gz
```

### 2. 恢复

```bash
gunzip < /backups/sparkle_20260110.sql.gz | docker exec -i sparkle_db psql -U postgres sparkle
```

---

## 🚨 紧急回滚

如果新版本部署失败：

1.  **修改 Image Tag**: 在 `docker-compose.prod.yml` 中改回上一个稳定版本 tag。
2.  **重启服务**: `docker compose up -d`
3.  **数据库回滚** (如果涉及):
    ```bash
    docker exec -it ai_engine alembic downgrade -1
    ```

---

*文档维护者: DevOps Team*