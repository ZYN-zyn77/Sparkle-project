# Sparkle 生产部署指南

> **版本**: v1.0
> **状态**: Production Ready
> **最后更新**: 2025-12-27

---

## 📋 部署清单

### ✅ 部署前检查

- [ ] 环境变量配置完成
- [ ] 数据库迁移已执行
- [ ] Redis 服务可用
- [ ] SSL 证书已配置
- [ ] 监控系统已就绪
- [ ] 备份策略已制定
- [ ] 回滚方案已准备

---

## 🔧 环境配置

### 1. 核心环境变量

创建 `.env.production` 文件：

```bash
# ==================== 核心配置 ====================
APP_NAME=Sparkle
APP_VERSION=0.3.0
DEBUG=false

# ==================== 网络配置 ====================
GRPC_PORT=50051
HTTP_PORT=8000
GATEWAY_PORT=8080
BACKEND_CORS_ORIGINS=["https://your-domain.com"]

# ==================== 数据库配置 ====================
DATABASE_URL=postgresql://user:password@host:5432/sparkle
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=30

# ==================== Redis 配置 ====================
REDIS_URL=redis://localhost:6379/0
REDIS_POOL_SIZE=50

# ==================== LLM 配置 ====================
LLM_PROVIDER=deepseek
LLM_API_BASE_URL=https://api.deepseek.com/v1
LLM_API_KEY=sk-xxx
LLM_MODEL_NAME=deepseek-chat
LLM_TIMEOUT=60

# ==================== 安全配置 ====================
SECRET_KEY=your-very-long-secret-key-min-32-chars
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440

# ==================== 性能配置 ====================
MAX_CONCURRENT_SESSIONS=100
CIRCUIT_BREAKER_THRESHOLD=5
CIRCUIT_BREAKER_TIMEOUT=60
DAILY_QUOTA=100000

# ==================== 日志配置 ====================
LOG_LEVEL=INFO
LOG_FORMAT=json
LOG_FILE=/var/log/sparkle/app.log

# ==================== 监控配置 ====================
ENABLE_METRICS=true
ENABLE_TRACING=true
```

### 2. 配置验证

```bash
cd backend
python -m app.config_production
```

---

## 🐳 Docker 部署

### 1. Docker Compose 配置

```yaml
# docker-compose.production.yml
version: '3.8'

services:
  # PostgreSQL
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: sparkle
      POSTGRES_USER: sparkle
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sparkle"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis
  redis:
    image: redis:7-alpine
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

  # Python gRPC Server
  grpc-server:
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      - DATABASE_URL=postgresql://sparkle:${DB_PASSWORD}@postgres:5432/sparkle
      - REDIS_URL=redis://redis:6379/0
      - ${ENV_FILE}
    command: python grpc_server.py
    ports:
      - "50051:50051"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2G
        reservations:
          cpus: '1'
          memory: 1G

  # Go Gateway
  gateway:
    build:
      context: ./backend/gateway
      dockerfile: Dockerfile
    environment:
      - DATABASE_URL=postgresql://sparkle:${DB_PASSWORD}@postgres:5432/sparkle
      - REDIS_URL=redis://redis:6379/0
      - AGENT_ADDRESS=grpc-server:50051
      - ${ENV_FILE}
    ports:
      - "8080:8080"
    depends_on:
      - grpc-server
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 1G

  # Python Backend (REST API)
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      - DATABASE_URL=postgresql://sparkle:${DB_PASSWORD}@postgres:5432/sparkle
      - REDIS_URL=redis://redis:6379/0
      - ${ENV_FILE}
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2G

  # Prometheus
  prometheus:
    image: prom/prometheus:latest
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus_data:/prometheus
    ports:
      - "9090:9090"
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/etc/prometheus/console_libraries'
      - '--web.console.templates=/etc/prometheus/consoles'
      - '--storage.tsdb.retention.time=30d'
      - '--web.enable-lifecycle'
    restart: unless-stopped

  # Grafana
  grafana:
    image: grafana/grafana:latest
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_PASSWORD}
    volumes:
      - grafana_data:/var/lib/grafana
      - ./monitoring/grafana/dashboards:/etc/grafana/provisioning/dashboards
      - ./monitoring/grafana/datasources:/etc/grafana/provisioning/datasources
    ports:
      - "3000:3000"
    restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
  prometheus_data:
  grafana_data:
```

### 2. 部署命令

```bash
# 1. 设置环境变量
export ENV_FILE=.env.production
export DB_PASSWORD=your_secure_password
export GRAFANA_PASSWORD=your_grafana_password

# 2. 构建并启动
docker-compose -f docker-compose.production.yml up -d --build

# 3. 查看日志
docker-compose -f docker-compose.production.yml logs -f

# 4. 检查状态
docker-compose -f docker-compose.production.yml ps
```

---

## 🚀 Systemd 部署（可选）

### 1. Python gRPC Server

```ini
# /etc/systemd/system/sparkle-grpc.service
[Unit]
Description=Sparkle gRPC Server
After=network.target postgresql.service redis.service

[Service]
Type=simple
User=sparkle
Group=sparkle
WorkingDirectory=/opt/sparkle/backend
Environment=PATH=/opt/sparkle/backend/venv/bin
Environment=DATABASE_URL=postgresql://sparkle:password@localhost:5432/sparkle
Environment=REDIS_URL=redis://localhost:6379/0
EnvironmentFile=/opt/sparkle/.env.production
ExecStart=/opt/sparkle/backend/venv/bin/python grpc_server.py
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

### 2. Go Gateway

```ini
# /etc/systemd/system/sparkle-gateway.service
[Unit]
Description=Sparkle Go Gateway
After=network.target sparkle-grpc.service

[Service]
Type=simple
User=sparkle
Group=sparkle
WorkingDirectory=/opt/sparkle/backend/gateway
EnvironmentFile=/opt/sparkle/.env.production
ExecStart=/opt/sparkle/backend/gateway/bin/gateway
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

### 3. 启动服务

```bash
sudo systemctl daemon-reload
sudo systemctl enable sparkle-grpc sparkle-gateway
sudo systemctl start sparkle-grpc sparkle-gateway

# 查看状态
sudo systemctl status sparkle-grpc
sudo systemctl status sparkle-gateway

# 查看日志
sudo journalctl -u sparkle-grpc -f
sudo journalctl -u sparkle-gateway -f
```

---

## 📊 监控配置

### 1. Prometheus 配置

```yaml
# monitoring/prometheus.yml
global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "alert_rules.yml"

scrape_configs:
  - job_name: 'sparkle-backend'
    static_configs:
      - targets: ['backend:8000']
    metrics_path: '/metrics'
    scrape_interval: 10s

  - job_name: 'sparkle-gateway'
    static_configs:
      - targets: ['gateway:8080']
    metrics_path: '/metrics'
    scrape_interval: 10s

  - job_name: 'postgres'
    static_configs:
      - targets: ['postgres:5432']
    scrape_interval: 30s

  - job_name: 'redis'
    static_configs:
      - targets: ['redis:6379']
    scrape_interval: 15s
```

### 2. 告警规则

```yaml
# monitoring/alert_rules.yml
groups:
  - name: sparkle_alerts
    rules:
      - alert: HighErrorRate
        expr: rate(http_requests_total{status=~"5.."}[5m]) > 0.1
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "High error rate detected"

      - alert: CircuitBreakerOpen
        expr: chat_orchestrator_circuit_breaker == 1
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Circuit breaker is open"

      - alert: HighMemoryUsage
        expr: process_resident_memory_bytes > 2 * 1024 * 1024 * 1024
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "Memory usage exceeds 2GB"

      - alert: QueueBacklog
        expr: redis_queue_length > 500
        for: 10m
        labels:
          severity: warning
        annotations:
          summary: "Queue backlog detected"
```

---

## 🔍 健康检查

### 1. API 端点

```bash
# 基础健康检查
curl https://api.your-domain.com/api/v1/health

# 详细健康检查
curl https://api.your-domain.com/api/v1/health/detailed

# 就绪检查（K8s）
curl https://api.your-domain.com/api/v1/health/ready

# 存活检查（K8s）
curl https://api.your-domain.com/api/v1/health/live

# 队列状态
curl https://api.your-domain.com/api/v1/health/queue/status

# Prometheus 指标
curl https://api.your-domain.com/api/v1/health/metrics
```

### 2. 监控脚本

```bash
#!/bin/bash
# health-check.sh

URL="https://api.your-domain.com/api/v1/health"
ALERT_WEBHOOK="https://hooks.slack.com/services/YOUR/WEBHOOK"

while true; do
    RESPONSE=$(curl -s -w "\n%{http_code}" $URL)
    HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
    BODY=$(echo "$RESPONSE" | sed '$d')

    if [ "$HTTP_CODE" != "200" ]; then
        curl -X POST $ALERT_WEBHOOK \
            -H 'Content-Type: application/json' \
            -d "{\"text\":\"🚨 Health check failed: $HTTP_CODE\"}"
    fi

    sleep 60
done
```

---

## 🛡️ 安全配置

### 1. Nginx 反向代理

```nginx
# /etc/nginx/sites-available/sparkle
server {
    listen 80;
    server_name api.your-domain.com;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name api.your-domain.com;

    # SSL 配置
    ssl_certificate /etc/ssl/certs/your-domain.crt;
    ssl_certificate_key /etc/ssl/private/your-domain.key;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    # 安全头
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # WebSocket 支持
    map $http_upgrade $connection_upgrade {
        default upgrade;
        '' close;
    }

    # Gateway (WebSocket + API)
    location /ws/chat {
        proxy_pass http://localhost:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400;
    }

    location /api/v1 {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Python Backend (Direct access if needed)
    location /docs {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
    }

    location /uploads {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
    }

    # Health check endpoint
    location /health {
        access_log off;
        proxy_pass http://localhost:8080/api/v1/health;
        proxy_set_header Host $host;
    }
}
```

### 2. 防火墙规则

```bash
# 只开放必要端口
ufw allow 22/tcp    # SSH
ufw allow 80/tcp    # HTTP
ufw allow 443/tcp   # HTTPS
ufw allow 5432/tcp  # PostgreSQL (internal only)
ufw allow 6379/tcp  # Redis (internal only)
ufw enable
```

---

## 📈 性能调优

### 1. PostgreSQL 调优

```conf
# /etc/postgresql/16/main/postgresql.conf

# 连接设置
max_connections = 200
shared_buffers = 2GB
effective_cache_size = 6GB

# 日志
log_min_duration_statement = 500
log_checkpoints = on
log_connections = on
log_disconnections = on

# 自动清理
autovacuum = on
autovacuum_max_workers = 4
autovacuum_naptime = 15s
```

### 2. Redis 调优

```conf
# /etc/redis/redis.conf
maxmemory 2gb
maxmemory-policy allkeys-lru
tcp-keepalive 300
timeout 300
```

### 3. 系统调优

```bash
# 增加文件描述符限制
echo "* soft nofile 65536" >> /etc/security/limits.conf
echo "* hard nofile 65536" >> /etc/security/limits.conf

# TCP 调优
echo "net.core.somaxconn = 1024" >> /etc/sysctl.conf
echo "net.ipv4.tcp_max_syn_backlog = 2048" >> /etc/sysctl.conf
sysctl -p
```

---

## 🔄 部署流程

### 1. 蓝绿部署

```bash
# 1. 部署新版本到绿色环境
docker-compose -f docker-compose.green.yml up -d

# 2. 健康检查
./health-check.sh green

# 3. 流量切换（Nginx）
# 修改 Nginx 配置，将流量从蓝色切换到绿色

# 4. 监控指标
# 观察 15-30 分钟，确认无异常

# 5. 保留蓝色环境作为回滚备份
```

### 2. 回滚流程

```bash
# 如果发现问题，立即回滚
# 1. 切换流量回蓝色环境
sudo nginx -s reload

# 2. 停止绿色环境
docker-compose -f docker-compose.green.yml down

# 3. 分析问题
docker-compose -f docker-compose.green.yml logs > rollback_analysis.log
```

---

## 🔒 P0/P1 安全修复配置（2025-12-28 更新）

### 1. 向量索引配置 (P0)

**状态**: ✅ 自动通过 Alembic 迁移应用

```bash
# 确保向量索引已创建
cd backend
alembic upgrade head

# 验证索引
psql $DATABASE_URL -c "
SELECT schemaname, tablename, indexname
FROM pg_indexes
WHERE indexname LIKE '%embedding%' OR indexname LIKE '%hnsw%';"
```

预期输出：
- `idx_knowledge_nodes_embedding_hnsw` (HNSW vector_cosine_ops)
- `idx_cognitive_fragments_embedding_hnsw` (HNSW vector_cosine_ops)
- `idx_chat_messages_session_created` (B-Tree composite)

### 2. 幂等性系统配置 (P1)

**必需环境变量**:

```bash
# 选择幂等性存储后端（推荐: redis）
IDEMPOTENCY_STORE=redis        # 选项: memory, redis, database

# Redis 后端需要 Redis 可用
REDIS_URL=redis://user:password@redis-host:6379/0

# 数据库后端（如果选择 database）
DATABASE_URL=postgresql://user:password@db-host:5432/sparkle
```

**验证方式**:

```bash
# 测试幂等性缓存
curl -X POST http://localhost:8000/api/v1/chat/stream \
  -H "Idempotency-Key: test-123" \
  -H "Authorization: Bearer $TOKEN"

# 第二次请求同一 key 应返回缓存响应
curl -X POST http://localhost:8000/api/v1/chat/stream \
  -H "Idempotency-Key: test-123" \
  -H "Authorization: Bearer $TOKEN"
```

### 3. gRPC 安全配置 (P1)

**必需环境变量**:

```bash
# 生产环境强制 TLS
GRPC_REQUIRE_TLS=true
GRPC_TLS_CERT_PATH=/etc/sparkle/grpc/cert.pem
GRPC_TLS_KEY_PATH=/etc/sparkle/grpc/key.pem

# 禁用反射（仅调试时启用）
GRPC_ENABLE_REFLECTION=false      # 或完全省略此变量

# Gateway 到 Agent 的 TLS
AGENT_TLS_ENABLED=true
AGENT_TLS_CA_CERT=/etc/sparkle/grpc/ca-cert.pem
AGENT_TLS_SERVER_NAME=agent.internal
```

**生成自签名证书**（仅开发）:

```bash
# 生产环境应使用受信的证书颁发机构
openssl req -x509 -newkey rsa:4096 -keyout key.pem -out cert.pem -days 365 -nodes
```

**验证 TLS 连接**:

```bash
# 验证 gRPC TLS
grpcurl -cacert /path/to/ca.pem \
  list \
  localhost:50051
```

### 4. 密钥与密码安全 (P1)

**必需环境变量**:

```bash
# SECRET_KEY: 生产环境强制设置（最少 32 字符）
SECRET_KEY=your-very-long-secret-key-min-32-chars

# JWT_SECRET: 生产环境强制设置
JWT_SECRET=your-jwt-secret-key-min-32-chars

# 生产环境强制禁用 DEBUG
DEBUG=false
ENVIRONMENT=production

# 数据库密码：禁止使用默认密码
DATABASE_URL=postgresql://prod_user:STRONG_PASSWORD@db-host:5432/sparkle
```

**密钥生成脚本**:

```bash
# 生成 SECRET_KEY
openssl rand -base64 32

# 生成 JWT_SECRET
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

### 5. Demo 模式禁用 (P1)

**移动端生产构建**:

```bash
# 生产构建：Demo 模式默认禁用
flutter build apk --release
flutter build ios --release

# 如需 Demo 模式，显式启用
flutter run --dart-define=DEMO_MODE=true

# 后端 Demo 模式
DEMO_MODE=false        # 默认配置
```

### 6. 启动验证检查表

```bash
# 1. 验证 SECRET_KEY 已设置
test -n "$SECRET_KEY" && echo "✅ SECRET_KEY set" || echo "❌ SECRET_KEY missing"

# 2. 验证 DEBUG=false
[ "$DEBUG" = "false" ] && echo "✅ DEBUG disabled" || echo "❌ DEBUG enabled"

# 3. 验证 gRPC TLS
test -f "$GRPC_TLS_CERT_PATH" && echo "✅ GRPC cert exists" || echo "❌ GRPC cert missing"

# 4. 验证幂等性存储
curl -s http://localhost:8000/health | jq .idempotency_store

# 5. 验证向量索引
psql $DATABASE_URL -c "SELECT COUNT(*) FROM pg_indexes
  WHERE indexname LIKE '%embedding%';"
```

---

## 🚨 故障排查

### 1. 常见问题

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 数据库连接失败 | 密码错误/网络不通 | 检查 DATABASE_URL 和网络 |
| Redis 连接失败 | Redis 未启动 | `systemctl start redis` |
| gRPC 超时 | Python 服务崩溃 | 检查日志，重启服务 |
| 熔断器开启 | 失败率过高 | 检查下游服务，等待自动恢复 |
| 高内存使用 | 内存泄漏 | 重启服务，检查代码 |

### 2. 日志分析

```bash
# 查看错误日志
journalctl -u sparkle-grpc -n 100 | grep ERROR

# 实时监控
tail -f /var/log/sparkle/app.log | jq .

# 性能分析
python -m cProfile -o profile.stats grpc_server.py
```

---

## 📞 运维联系

- **紧急支持**: ops@your-domain.com
- **技术文档**: https://docs.your-domain.com
- **监控面板**: https://grafana.your-domain.com
- **日志系统**: https://logs.your-domain.com

---

## 📝 版本历史

| 版本 | 日期 | 说明 | 作者 |
|------|------|------|------|
| 1.1 | 2025-12-28 | 添加 P0/P1 安全修复配置指南 | Claude Code |
| 1.0 | 2025-12-27 | 初始生产部署指南 | Claude Code |
